# Core dependencies for fond-reel-masters tools
# Install with: pip install -r requirements.txt

# Image processing (contact sheets, perceptual hashing)
pillow>=10.0.0

# Perceptual image hashing for protagonist face-drift detection
# Used by tools/verify_protagonist.py (_average_hash)
imagehash>=4.3.1

# FFmpeg Python bindings (used alongside system ffmpeg)
ffmpeg-python>=0.2.0
